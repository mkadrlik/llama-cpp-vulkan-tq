name: Mirror to GitHub
on:
  push:
    branches: [ main ]
  schedule:
    - cron: '0 */6 * * *'

jobs:
  mirror:
    runs-on: [self-hosted, rocm]
    steps:
      - name: Cleanup
        run: rm -rf /tmp/llama-cpp-nvidia-tq-mirror

      - name: Checkout
        run: |
          git clone "${{ vars.GITEA_URL }}/mkadrlik/llama-cpp-nvidia-tq.git" /tmp/llama-cpp-nvidia-tq-mirror

      - name: Push to GitHub
        run: |
          cd /tmp/llama-cpp-nvidia-tq-mirror
          git remote add github "https://${{ secrets.GHCR_TOKEN }}@github.com/mkadrlik/llama-cpp-nvidia-tq.git" 2>/dev/null || true
          git push github main --tags
