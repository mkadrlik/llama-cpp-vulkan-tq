###############################################################################
# llama-cpp-vulkan-rocm — Docker Compose
#
# Vulkan + ROCm hybrid backend for fast inference.
# TriAttention (CUDA-only) is patched out via #ifdef GGML_CUDA guards.
# Flash Attention (-fa on) is enabled by default — required for quantized models.
###############################################################################

services:
  llama-server:
    image: ${IMAGE_REGISTRY}/${IMAGE_NAME}:${IMAGE_TAG}
    container_name: llama-cpp-vulkan-rocm
    restart: unless-stopped
    devices:
      - /dev/dri:/dev/dri
    ports:
      - "${SERVER_PORT}:8080"
    volumes:
      - "${MODEL_PATH}:${MODEL_MOUNT}:ro"
    command: >
      --host ${SERVER_HOST}
      --port 8080
      --model ${MODEL_MOUNT}
      -fa on
      -ngl ${GPU_LAYERS}
      ${THREADS:+-t ${THREADS}}
      ${CONTEXT_SIZE:+-c ${CONTEXT_SIZE}}
      ${EXTRA_ARGS}
